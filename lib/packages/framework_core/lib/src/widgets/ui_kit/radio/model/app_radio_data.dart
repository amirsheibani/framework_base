import 'package:flutter/widgets.dart';

class AppRadioData{
  final String title;
  final String? description;
  final Widget? trailing;

  AppRadioData({required this.title, this.description, this.trailing});
}